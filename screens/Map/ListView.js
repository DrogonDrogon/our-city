import React from 'react';
import { ScrollView, Text } from 'react-native';

class ListView extends React.Component {
  render() {
    return (
      <ScrollView>
        <Text>test my listview</Text>
      </ScrollView>
    );
  }
}

export default ListView;
